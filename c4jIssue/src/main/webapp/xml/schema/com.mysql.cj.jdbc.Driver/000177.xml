<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE DDL SYSTEM "schema.dtd">
<DDL>
	
	
	<statement>
		UPDATE APP_PALLET SET EQUIPMENT_TYPE = 
			COALESCE((SELECT EQUIPMENT_TYPE FROM APP_MATERIAL WHERE APP_MATERIAL.MATERIAL = APP_PALLET.MATERIAL),'') 
			WHERE COALESCE(APP_PALLET.EQUIPMENT_TYPE,'') = ''
	</statement>
		
	<statement>
		UPDATE APP_PALLET_HISTORY SET EQUIPMENT_TYPE = 
			COALESCE((SELECT EQUIPMENT_TYPE FROM APP_PALLET WHERE APP_PALLET.SSCC = APP_PALLET_HISTORY.SSCC AND APP_PALLET.EQUIPMENT_TYPE > '' ),'')
			WHERE COALESCE(APP_PALLET_HISTORY.EQUIPMENT_TYPE,'') = '' 
	</statement>
		
</DDL>
