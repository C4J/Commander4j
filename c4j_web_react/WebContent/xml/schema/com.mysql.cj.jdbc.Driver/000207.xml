<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE DDL SYSTEM "schema.dtd">
<DDL>

	<statement>
		ALTER TABLE `SYS_MODULES` 
			ADD COLUMN `PRINTER_TYPE` varchar(30) NULL AFTER `AUTO_LABELLER_LABEL_FILE`,
			ADD COLUMN `GROUP_ID` varchar(15) NULL AFTER `PRINTER_TYPE`,
			ADD COLUMN `PRINTER_DPI` varchar(10) NULL AFTER `GROUP_ID`,
			ADD COLUMN `LANGUAGE` varchar(15) NULL AFTER `PRINTER_DPI`,
			ADD COLUMN `ENABLE_DIRECT_PRINT` varchar(1) NULL AFTER `LANGUAGE`;
	</statement>

	<statement>
		UPDATE SYS_MODULES SET PRINTER_TYPE = '', GROUP_ID = '', PRINTER_DPI = '', LANGUAGE = '',ENABLE_DIRECT_PRINT = 'N'
	</statement>
	
	<statement>
		ALTER TABLE `SYS_MODULES` 
			MODIFY COLUMN `PRINTER_TYPE` 	varchar(30) CHARACTER SET {charset} COLLATE {collation} NULL DEFAULT '' AFTER `AUTO_LABELLER_LABEL_FILE`,
			MODIFY COLUMN `GROUP_ID` 		varchar(15) CHARACTER SET {charset} COLLATE {collation} NULL DEFAULT '' AFTER `PRINTER_TYPE`,
			MODIFY COLUMN `PRINTER_DPI` 	varchar(10) CHARACTER SET {charset} COLLATE {collation} NULL DEFAULT '' AFTER `GROUP_ID`,
			MODIFY COLUMN `LANGUAGE` 		varchar(15) CHARACTER SET {charset} COLLATE {collation} NULL DEFAULT '' AFTER `PRINTER_DPI`,
			MODIFY COLUMN `ENABLE_DIRECT_PRINT` varchar(1) CHARACTER SET {charset} COLLATE {collation}  NULL DEFAULT 'N' AFTER `LANGUAGE`;
	</statement>
	
	<statement>
		CREATE TABLE `SYS_WORKSTATION_PREFS` (
		  `WORKSTATION` varchar(40)  CHARACTER SET {charset} COLLATE {collation} NOT NULL DEFAULT '',
		  `MODULE_ID`   varchar(25)  CHARACTER SET {charset} COLLATE {collation} NOT NULL DEFAULT '',
		  `DEVICE_TYPE` varchar(30)  CHARACTER SET {charset} COLLATE {collation} DEFAULT '',
		  `PRINTER_ID`  varchar(255) CHARACTER SET {charset} COLLATE {collation} DEFAULT '',
		  PRIMARY KEY (`WORKSTATION` DESC,`MODULE_ID` DESC) USING BTREE
		) ENGINE=InnoDB DEFAULT CHARSET={charset} COLLATE={collation};
	</statement>

</DDL>
